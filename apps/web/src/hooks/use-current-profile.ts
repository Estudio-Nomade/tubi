/**
 * Re-export for the conventional hooks/ import path.
 * Implementation lives next to SessionProvider.
 */
export {
  useCurrentProfile,
  type SessionSnapshot,
} from "@/components/auth/session-provider";
